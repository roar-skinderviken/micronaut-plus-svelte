micronaut:
  application:
    name: micronaut-plus-react

  views:
    folder: static

  router:
    static-resources:
      default:
        enabled: true
        mapping: /*.*
        paths:
          - classpath:static

      assets:
        enabled: true
        mapping: /assets/**
        paths:
          - classpath:static/assets